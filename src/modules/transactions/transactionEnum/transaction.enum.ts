export enum TransactionTypes {
  PAYOUT = "payout",
  TRANSFER = "transfer",
  DEPOSIT = "deposit",
  REVERSAL = "reversal",
}

export enum TransactionStatus {
  PENDING = "pending",
  SUCCESSFUL = "successful",
  FAILED = "failed",
}

export enum DbSort {
  DESC = "DESC",
  ASC = "ASC",
}
